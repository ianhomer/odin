# Test specific application properties

spring.profiles.active = test

# Help with seeing how spring contexts are spun up during tests
# org.springframework.test.context.cache = INFO


# Configuration for test execution
# TODO : Make this a delta over and above main, currently it needs repeated property definitions
# in here.

spring.data.rest.base-path = /api/rest
spring.jpa.hibernate.use-new-id-generator-mappings = true
spring.jpa.properties.hibernate.format_sql = false
spring.jpa.show-sql = false
spring.thymeleaf.cache = false




